# bookbot

python command-line app that analyzes the contents of a txt file

**what it does**:
- counts total words in the document
- counts frequency of each character
- outputs a sorted report to the terminal

**how to run**:
```
python3 main.py <path-to-text-file>
```

built with: python
